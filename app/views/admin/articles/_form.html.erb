<%= simple_form_for [:admin, @article] do |f| %>

  <%= f.input :title  %>
  <%= f.input :text, as: :text, :input_html => { :rows => 10 } %>
  <%= f.input :active %>

  <%= f.submit "Create article", class: "btn btn-default btn-success" %>

<% end %>
